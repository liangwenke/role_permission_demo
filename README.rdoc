== Rails Application Role Dome

  The role assciation include: Admin, Role, Permission, Audit(check operation log)
  
  
== Install

  rake db:drop && rake db:create && rake db:migrate && rake db:seed
  
  ruby script/server
  
  login: technology/123456

